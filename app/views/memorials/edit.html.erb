<% if current_user != nil && @memorial != nil && ( @memorial.user == current_user || current_user.role == "admin" ) %>

    <div class="form-page">
      <%= render 'form' %><br>
      <%= link_to 'Destroy', @memorial, method: :delete, data: { confirm: 'Are you sure?' } %>
    </div>

<% end %>
